version: '3.8'

networks:
  everydaily:
   external: true
  
services:
  everydaily-db:
    image: postgres:latest
      container_name: everydaily-db
      hostname: everydaily-db
      ports:
        - "5432:5432"
      environment:
        POSTGRES_USER: admin
        POSTGRES_PASSWORD: P@ssw0rd
      networks:
        - everydaily
      restart: always

  # dek-elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
  #   container_name: dek-elasticsearch
  #   restart: always
  #   environment:
  #     - xpack.security.enabled=false
  #     - discovery.type=single-node
  #     - ES_JAVA_OPTS=-Xms512m -Xmx512m
  #   ulimits:
  #     memlock:
  #       soft: -1
  #       hard: -1
  #     nofile:
  #       soft: 65536
  #       hard: 65536
  #   cap_add:
  #     - IPC_LOCK
  #   ports:
  #     - 9200:9200
  #     - 9300:9300
  #   networks:
  #     - dek
  #   hostname: "elasticsearch"

  dek-redis:
    image: redis
    container_name: dek-redis
    ports:
      - 6379:6379
    restart: always
    hostname: "redis"
    networks:
      - everydaily

  dek-rabbitmq: # login guest:guest
    image: rabbitmq:3-management
    hostname: "rabbitmq"
    labels:
      NAME: "rabbitmq"
    container_name: "dek-rabbitmq"
    ports:
    - "5672:5672"
    - "15672:15672"
    restart: always
    networks:
      - everydaily